<%= form_for @playlist_post, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :playlist_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :playlist_id, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :post_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :post_id, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                playlist_posts_path, :class => 'btn' %>
  </div>
<% end %>
